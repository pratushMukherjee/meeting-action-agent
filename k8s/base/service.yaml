apiVersion: v1
kind: Service
metadata:
  name: meeting-agent-api
  labels:
    app: meeting-agent
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  selector:
    app: meeting-agent
    component: api
